name: MidiCore.CI

trigger:
- master

resources:
- repo: self
  clean: true

jobs:
- template: build.yml
  parameters:
    name: MidiCoreCI
    artifactFileName: '$(Build.ArtifactStagingDirectory)/MidiCore-CI.zip'
    pool:
      vmImage: 'Ubuntu 16.04'
